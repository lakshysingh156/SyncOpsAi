kind = "web"
previewPath = "/"
title = "SyncOps AI"
version = "1.0.0"
id = "artifacts/syncops-ai"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23500

[services.development]
run = "pnpm --filter @workspace/syncops-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/syncops-ai", "run", "build" ]
publicDir = "artifacts/syncops-ai/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23500"
BASE_PATH = "/"
